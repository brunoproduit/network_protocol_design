#TODO: ACKing of Messages --> Into the backgroundprocesses.py (Listener)

#TODO: Router (it's not really working for me) --> Touple

#TODO: Format/Decoding of the MD5 Addresses --> Other group

#TODO: Sending and encrypting files --> Last piece

#TODO: Chunking --> Started
# * Class Chunking Enginee - A thread
# --> check_integrity
# --> get_chunks(packet) --> splits the before created packet into a list/array of chunks that can be sent
# --> add_chunk(chunk) --> adds chunks to a property of the enginee (e.g. packet)
# --> get_packet() --> checks if all chunks for the packet are present, then returns the whole packet
